import { style } from "@vanilla-extract/css"

export const container = style({
    display: "flex",
    padding: "1rem 12px",
    alignItems: "stretch",
    flexDirection: "column",
    gap: "0.5rem",
})
