import { style } from "@vanilla-extract/css"

export const appShellScreen = style({
    display: "flex",
    flexDirection: "column",
    minHeight: "100vh",
    width: "100%",
    maxWidth: "100vw",
})
