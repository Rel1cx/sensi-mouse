import { style } from "@vanilla-extract/css"

export const appShellScreen = style({
    display: "flex",
    flexDirection: "column",
    width: "100%",
    maxWidth: "100vw",
    minHeight: "100vh",
})
